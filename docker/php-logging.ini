; Always log PHP errors to stderr so they appear in `docker logs`.
log_errors = On
error_log = /proc/self/fd/2
error_reporting = E_ALL
; Never print notices/warnings/deprecations to the HTTP response.
display_errors = Off
display_startup_errors = Off
